# dependencies
node_modules/
.pnpm-store/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# test / coverage
coverage/
playwright-report/
test-results/

# env & secrets
.env
.env.*
!.env.example
!.env.sample

# local Nixploy runtime (Traefik YAML, compose files, logs, metrics, ACME)
.nixploy-data/
# The maintainer's personal helper at the repo root only — anchored so it does
# not also swallow the committed tools/dev.sh.
/dev.sh

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# keys / certs that may appear during local setup
*.pem
*.key

# misc
.DS_Store
.turbo/
.vercel/
.idea/
.vscode/
*.swp
drizzle/meta/_journal.json.bak

# Next.js 16 dev server writes these agent guides next to each app
apps/*/AGENTS.md
apps/*/CLAUDE.md
