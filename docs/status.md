# Repo status & working backlog

Living document. Every work session (human or AI) should refresh the snapshot
and move items between the backlog sections. Durable knowledge belongs in
[`../CLAUDE.md`](../CLAUDE.md) / [`codebase-map.md`](./codebase-map.md); this
file is for **state** and **tasks**.

## Snapshot — 2026-09-11 (end of the audit implementation: Sprints A–E + final wave)

| Item | Value |
| --- | --- |
| Branches | `chore/sprint-1-hygiene` (14 commits over `main` @ `07ab3d4`) → `work/stability-and-landing` (16 more commits on top). **Neither pushed nor merged** — user decision. Merge order: sprint-1 first, then stability. |
| Tags / releases | PaaS `v0.1.0` (GitHub Release + GHCR), CLI `cli-v0.1.1` — unchanged. The stability branch is release-worthy as `v0.1.1`/`v0.2.0` once merged and CI is green. |
| Local toolchain | Node 22.21.0, pnpm 10.33.0, Docker 29.7.2, Swarm active |
| Key versions | Next 16.3.4 (web + landing), better-auth + api-key 1.7.3, React 19.2.8, Biome 2.5.12, zod 4.6.1, TanStack Query 5.102, lucide 1.43, commander 15, vitest 5.0.0, TypeScript 5.9.3, drizzle-orm 0.45.2 |
| `pnpm typecheck` | ✅ all 4 workspaces |
| `pnpm test` / `pnpm test:db` | ✅ 145 files, 1904 passed with `DATABASE_URL_TEST` (1 skipped) — DB-backed files run with `fileParallelism: false`; run the DB suite alone, it shares `nixploy_test`. CLI 91, web 65 |
| Biome (CI command, `--error-on-warnings`) + knip | ✅ 0 errors, 0 warnings; knip 5.88.1 clean |
| Builds | ✅ web (also without `DATABASE_URL`, the Docker scenario), landing (31 static routes), cli; Docker image built and booted healthy from a clean tree by the ops pass |
| REST e2e loop (`traefik/whoami` app → traefik.me domain → HTTPS via Traefik → stop/start/reload → env + port edit keeps inherited env → redeploy → rollback; uptime-kuma template → private net + Traefik → stop/start; compose safety probe → 400; postgres create/start/exec/stop/start/rename-guard/protected volume; docker/gitops/audit/monitoring/schedule (command + stdin script); cascade cleanup incl. volumes, YAML, dirs) | ✅ 58/59 on the final tree — the one miss is a resume artifact (latest deployment was the rollback), not a bug |
| UI verification | Both UI agents drove every touched page in light + dark (Playwright + in-app browser); 0 console / hydration errors after fixing three real hydration mismatches |
| Dev DB | `nixploy-dev-pg` (127.0.0.1:54329): `nixploy` (dev), `nixploy_test` (tenancy suite), `nixploy_e2e` (e2e user e2e@example.test) — all migrated to `0028` |
| Schema | 29 migrations (`0000`…`0028_ancient_shatterstar` = compose previews); 0026 layer-4 routing + engine versions + logical DBs, 0027 compose snapshots + template sources + image schedules + preview commit columns |
| Product | Phases 1–10 of `PLAN.md` shipped plus the 2026-09 audit plan; 44 routers / 390 procedures; 145 templates + org template sources; 11 notification providers; MCP with 32 tools; CLI 194+ registry commands |

## What the stability sweep did (2026-09-10/11)

Seven read-only audits (deploy engine, compose/DB/templates, Traefik/domains/previews, auth/tenancy/API, integrations/jobs, ops scripts, web UI ×4 partitions) produced ~120 confirmed findings; six fix passes plus a remote-server redesign landed them. Each commit body on `work/stability-and-landing` lists exactly what changed. Highlights, most important first:

- **Security**: compose files are rendered before validation (the `$VAR` / `~` / `volumes.name:` bypasses gave tenants host root or other tenants' volumes); tenant env never reaches the docker CLI; per-app private networks stop cross-tenant DNS; host uniqueness across orgs (route hijack); WS 2FA gate + Origin check + container-label ownership; instance backups instance-admin only; volume prune guards; update image validation/quoting; reserved app names; invitation binding; per-key rate limits.
- **Broken features made real**: HTTPS on the default certificate (502), stack deploys (rejected by the current CLI), remote servers (Swarm calls now hit the primary, tasks pinned by `node.id`), custom git SSH keys, previews (ports/volumes/port/auth/fork refs), rollbacks (were an empty tab), deploy notifications (never emitted), backup exit status (silent empty dumps) and restores > 96 KB, GitOps diff/apply semantics (TLS downgrade + redeploy-everything), `update.sh` (crashed on every run), database volume orphaning, mongo replicaSet (never started — now rejected), fork-PR previews.
- **UI**: capability gating everywhere (viewers stop seeing controls that 403), error states instead of misleading empty copy, correct invalidations, better-auth `{ error }` handling, setup-wizard retry, status polling after deploy, log viewer reconnect stop, hydration fixes.
- **Landing**: rebuilt (`apps/landing`) and then redesigned to a strict monochrome layout (centered hero + framed dashboard shot, stack marquee, stats, statement, code-to-production accordion, 2×3 features, security, plan cards shared with `/pricing`, two-column FAQ, CTA). Theme tokens in `globals.css` are greys/white only; `components/ui.tsx` holds the shared `Button`/`WindowFrame`/`SectionHeading` primitives.

## Dependency upgrade candidates (remaining majors)

| Package | Current → Latest | Risk | Notes |
| --- | --- | --- | --- |
| `typescript` | 5.9.3 → 7.0.2 | **High** | Go-based compiler; verify Biome/drizzle-kit/tsup/Next plugin. Nothing needs it. |
| `@tanstack/react-table` | 8.21.3 → 9.2.4 | Medium | `components/data-table/*` + services table. |
| `nodemailer` | 9.0.3 → 10.0.2 | Low-Med | Email provider only. |
| `motion` (landing) | 12.43 → 13.2 | Low | magicui primitives. |
| `node-os-utils` | 2.0.4 → 3.1.0 | Low | Host monitoring. |
| `react` / `react-dom` | 19.2.8 → 19.3.0 | Low-Med | Bump with a Next release that lists 19.3. |
| `drizzle-orm` | 0.45.2 → 1.0.0-rc | Deferred | Wait for stable + drizzle-kit pairing. |

Image pins to review periodically: `traefik:v3.5.0`, `postgres:17-alpine`, `node:22-alpine`, `traefik/whoami:v1.10.1`, template images (CI checks tags exist, not freshness).

## Known debt / follow-ups (from the sweep, not yet done)

- [x] Sprint B leftovers, all landed 2026-09-11: `CommandError` on `DomainError` + 18 router re-wraps deleted (`aaa7cef`), preview commit metadata + self-hosted commit links (`9415096`), streaming DB dumps (`7cb67ee`; instance/volume/redis archives still use the ~37 MB base64 pipeline).
- [x] Web toast sweep (2026-09-11): 127 → 44 `toast.error(` sites. The 44 left are deliberate — static validation, clipboard failures, the raw-fetch auth forms (already on `describeError`), template literals that embed `describeError(...)`, and 10 in `services/domain-manager.tsx` (owned by another branch at the time).
- [x] `overview-section.tsx` no longer polls `deployment.recent` every 10 s — it reads `useRunningDeployments()`, the one shared query (UX audit F31).
- [ ] **Release notes for the next tag**: builders now receive only `buildArgs` (`NIXPLOY_BUILD_WITH_RUNTIME_ENV=1` restores the merge); local commands time out after 30 min (`NIXPLOY_COMMAND_TIMEOUT_MS`); deploys have a 60 min deadline; `.env` is excluded from instance backups; Swarm joins and the manager role are instance-admin only; `compose.update` on a host-privileged stack is instance-admin only; six dead procedures removed (`application.saveDockerProvider` → use `application.saveSource`).

- [ ] **Compose previews follow-ups (2026-09-11)**: re-run a git-backed compose preview end to end now that local clones work again (the git half is unit-tested only); decide whether the compose preview switches move to the General tab next to the source settings; no public compose example passes the safety checks (`build:` / `ports:` / bind mounts all rejected) — a small Nixploy-owned sample repo would serve docs and e2e.
- [x] **Local Swarm VIP reset (2026-09-11)** — cleared by the Docker Desktop restart on 2026-09-12; the golden path routes through Traefik again. Kept for the record:: service VIPs on `nixploy-network` are unreachable from every container (task IPs work) — see the part-2 session line; recreate the overlay or restart Docker Desktop, then re-run the REST e2e.
- [ ] **Final-wave leftovers (2026-09-11)**: browser confirmation that an idle dashboard stops polling `deployment.recent` (push path verified through the WS layer only); a real `install.sh --split-worker` run on a throwaway host; the SSH breaker is in-memory (a persistent version needs `last_seen_at`/`last_error` columns + an `unreachable` enum value); `git` template sources are unit-tested only (`http-json` verified live); `compose.createFromUrl` has no UI; no CLI verbs for `compose.rollback`, `template.sources*`, `schedule.runOnce`; MCP could expose `list_template_sources`; (done 2026-09-11: server terminal on the pool, the two polls gated, landing docs synced); landing docs previously lagged `docs/install.md` (split worker, `NIXPLOY_ROLE`), the egress toggle + key-rotation runbook + audit export (`private-egress` help slug wanted), and `api-catalog.ts` misses the new procedures; `apps/cli`/`apps/web` still borrow vitest from `packages/server` (add the devDependency at the next `pnpm install`).
- [ ] **Remote servers**: stack-type compose on a remote must use registry-pullable images (`docker stack deploy` runs on the primary, no build); `docker.swarmServices` with a `serverId` returns the cluster-wide list (filter by `node.id` if per-server views are wanted); real multi-node smoke still pending (unit-tested only — no second host here).
- [ ] First-admin claim is serialized but the INSERT still happens after the hook; a fully atomic claim needs an `instance_setup` singleton row (migration).
- [ ] Redacted `env` / `composeFile` come back as `null`, indistinguishable from "unset"; add an explicit `redacted` flag to `one` responses (UI currently keys the read-only state on `can("secrets.read")`).
- [x] `lib/safe-next-path.ts` rejects any `?query` — fixed 2026-09-11 (`aaa7cef`), deep links survive login.
- [x] `schedule.create/remove` write audit rows since the hardening pass (`7cb67ee`: every mutation in 20 routers is script-verified to write exactly one row). (`settings/incidents/page.tsx` is now a `redirect()` stub into Monitoring, so the missing `<title>` is moot.)
- [x] Updater channel (2026-09-14, v0.2.3): version-tagged installs follow GitHub's newest release under the pin (`modules/updates/candidate.ts`); moving tags keep the digest comparison.
- [ ] `tools/release.sh` is untracked locally (`.git/info/exclude`) although docs reference it — decide whether to track it.
- [ ] Landing docs (`apps/landing/src/lib/docs/pages.ts`) still hand-duplicate `docs/*`; the sweep's operator-facing changes (TRUSTED_PROXIES, invitation header, PG 18, mongo replica sets, instance backups admin-only, GitOps partial apply, remote-server placement) are in `docs/` but not mirrored on the site yet.
- [ ] `NEXT_PUBLIC_APP_URL` third fallback in the GitHub callback; better-auth logs `ERROR … Failed to validate API key` on every bad key.
- [ ] Large files still unsplit: `git/webhook-handler.ts`, `databases/engine.ts`, `routers/application.ts`.
- [x] `deploymentStatus` has a `queued` value since migration 0019 (2026-09-11): queued rows are re-enqueued at boot, running rows are failed; one queued + one running job per app.
- [ ] **Dashboard hydration on soft navigation (2026-09-14)**: a hard load of `/dashboard` is clean, but navigating back to it from another page can still log one hydration mismatch inside `RecentDeployments` — the App Router replays an RSC payload rendered while the query was pending, while the browser QueryClient already holds the data, so one side paints the skeleton and the other the list. The always-on case (the subtitle branching on `isPending`) is fixed. The real fix is to prefetch the dashboard queries server-side into a `HydrationBoundary` instead of letting every list start pending; nothing else in the panel prefetches, so it is a deliberate piece of work, not a one-liner.
- [ ] Local swarm leftovers from earlier smokes: service `hello-ac7e44`, stale `.nixploy-data/metrics/*.jsonl` (harmless).

### UX sprint B — unified service pages (2026-09-11)

Landed in `apps/web` (uncommitted at the time of writing): UX audit F8/F9/F10/F11/F13/F14/F15/F16/F17/F26/F27/F31/F34.

- **One service page frame.** `components/services/service-page-header.tsx` is the single header for application, compose and the five database pages: breadcrumb `Projects / <project> / <environment> / <service>` (all linked, env link is `?env=<name>`), one status badge (the in-flight deployment badge replaces it while a build runs), and an action cluster driven by a `ServiceActions` array — `primary` actions stay visible at every width, the rest collapse into the `sm:hidden` overflow menu. Per-kind hooks (`use-application-actions.ts`, the compose/database mutations) are unchanged; they just build the array.
- **One tab order** for every kind: **General · [Compose file | Connection] · Deploy · Runtime · Domains · Environment · Backups · Advanced · Settings**, each kind rendering only the tabs it has. Advanced (mounts/ports/redirects/security/healthcheck/placement/swarm/rollbacks) is top-level and URL-synced as `?tab=advanced&advanced=<name>`.
- **Deep links survive.** `useSyncedTab` takes an `aliases` map; `SERVICE_TAB_ALIASES` (in the hook) maps the one retired id, `config` → `environment`. Everything else (`?tab=deployments|logs|advanced|backups|environment|…`) still resolves through each page's `SUB_TAB_PARENT` table. **Add a row to `SERVICE_TAB_ALIASES` whenever a service tab id is retired.**
- **Observability IA.** Incidents and the audit log are Monitoring tabs (`/dashboard/monitoring?tab=fleet|incidents|audit`); `settings/incidents` and `settings/activity` are `redirect()` stubs. `IncidentsView` / `ActivityView` / `MonitoringView` take `embedded` to drop their own `PageHeader`. Settings drops the "Observability" group and gains an instance-admin-only "Instance" group holding Platform.
- **Activity tray.** `components/layout/activity-tray.tsx` (bottom-right, in `AuthenticatedLayout`) lists running deployments from the shared query plus locally registered long operations — `useTrackedActivity({ active, … })` for a pending mutation, `trackActivity` / `settleActivity` for fire-and-forget ones. While it is on screen the body carries `data-activity-tray="1"` and `globals.css` lifts the sonner toast stack above it. **There is no org-wide "running backup runs" procedure** (`backup.runs` / `volumeBackup.runs` are per-id), so backups/restores only appear when started from a page that registers them — see the handoff if a server-side `backup.running` is added.
- **Discoverability.** The palette registers a "This Service" group (Logs / Domains / Environment / Terminal / Deploy this service) on any service route, plus Incidents, Audit log and a Documentation row; its cmdk `filter` scores the label above the keywords (a third of the weight), which is what fixed "dom" matching "New Database". Shortcuts: `g l` / `g o` / `g e` on service pages, `g d` Docker, `g p` projects, `g t` templates, `g m` monitoring, `g s` schedules, `g ,` settings — all listed in the `?` overlay together with a docs link. `components/ui/help-link.tsx` (`<HelpLink slug="…">` → `https://nixploy.com/docs/<slug>`; slugs must exist in `apps/landing/src/lib/docs/pages.ts`) is attached to Isolated deployment, Docker Stack vs Compose, Swarm role (×2), Publish mode, Traefik config, Reinstall, Auto updates, host-privileged templates, shared variables, watch paths and Swarm config.
- **Vocabulary.** Deployment `done` reads **Succeeded** everywhere (`status-badge.tsx`, `lib/status.ts#deploymentStatusLabel`, preview deployments, the dashboard list); `error` reads **Failed** in the dashboard list. Dates go through `<DateTime>` / `formatRelative` in deployment history, the update card, the backups lists and the dashboard.
- **Org accent (F34).** `OrgBrandingProvider` sets `--primary` **and** a luminance-derived `--primary-foreground` (a pale accent used to render white-on-white), plus `--brand`, which `UnderlineTabsTrigger` uses for the active-tab underline. `--brand` defaults to `--primary` in `globals.css`, so the panel stays monochrome until an accent is saved. The scope is spelled out in the branding card's helper text.

## Next steps

0. **Work the improvement audit** — [`audits/2026-09/README.md`](./audits/2026-09/README.md). Sprint A (§1, all 15 items) landed 2026-09-11 (commits `8b5c899`…`3844db6`); Sprint B landed 2026-09-11 (`fe7b566`…`89ca766`: error boundary, deploy provenance + pre-flight, backup run history + local destination + verify, half-built features, shared org env). Sprint C (network isolation + container hardening, durable queue + metrics store, Traefik middleware layer, unified service pages) in progress — the **unified service pages + discoverability** slice landed 2026-09-11 (see the UX sprint B section above: F8-F11, F13-F17, F26, F27, F31, F34); then §3.4+ (worker split, SSH pool, push events), §4 steps 2-5, remaining §2 (scoped keys/SSO, CLI/MCP parity, wildcard) and §6.
1. **Merge** `chore/sprint-1-hygiene` then `work/stability-and-landing` into `main`, push, let CI + Docker run; cut a release (`./tools/release.sh paas --bump minor`) — the sweep changes operator-visible behaviour (installer env, compose rendering, remote servers), so `v0.2.0` is the honest number.
2. **Real multi-node test** of the remote-server path on a second Linux host (join as worker, pin an app + a database, deploy, stop/start, remove server).
3. Mirror the operator-facing doc changes onto the landing docs.
4. Follow-ups above, smallest first; then the remaining major upgrades.

## Session log

- **2026-09-17 (competitor names removed from the repo)** — at the maintainer's request, no file names a competing product any more. The two migration guides were merged into one neutral [`migrate-from-another-panel.md`](./migrate-from-another-panel.md) (the per-panel concept maps collapse into a single "elsewhere → Nixploy" table, so nothing was lost) and the named files deleted; every link to them was updated in `README.md`, `docs/README.md`, `docs/getting-started.md`, the agent skill and the landing docs. Prose went generic in `PLAN.md` (whose title and premise both named one), `CLAUDE.md`, `docs/{auth,releases,status}.md`, the 2026-09 audit README and `product-gaps.md` (the comparison column now reads "the incumbent" / "another panel"), `install.sh`, the release workflow, the issue template and four code comments. Landing copy changed in `site.ts` (the About credit), `landing-data.ts`, `features/page.tsx` and the `migrate` docs page — **the public slug `/docs/migrate` is unchanged**, so no URL broke; only the title and body copy did. Verified: zero matches for either name anywhere outside `node_modules`/`.git`, every relative `.md` link resolves (which also turned up a pre-existing broken `../PLAN.md` in `docs/archive/next.md`, now fixed), the landing site builds and still prerenders `/docs/migrate`, typecheck 4/4, Biome 0/0, 2259 tests passing.

- **2026-09-17 (compose parity: three of four pieces)** — the compose half of the v0.3 slice in [`roadmap.md`](./roadmap.md), which is the prerequisite for any importer. **Auto-deploy was inert**: the switch, the columns and the pure matcher all existed, but `handleGitWebhook`'s push branch only ever queried `applications` and returned a hard-coded `composeIds: []`. A git-backed stack carries the identical provider/repository/owner/branch/autoDeploy/watchPaths columns, so the fix was to run the same predicate over both tables (renamed `applicationMatchesWebhook` to `serviceMatchesWebhook` to say so) and enqueue through a new `queueWebhookComposeDeployment`. Verified with signed GitHub deliveries against a seeded stack: a matching push queues a compose deployment with full provenance, auto-deploy off queues nothing, and watch paths `src/**` against a changed `docker-compose.yml` correctly matches nothing while `docker-compose.yml` matches. **Cancel was application-only**: `cancelDeployment` in the module never cared about kind, but the procedure resolved tenancy through `deployment.application` alone, so a compose build could not be stopped from any surface. It now resolves through whichever parent the row has and keeps its name, so panel, CLI and MCP callers are untouched; the compose Deployments tab passes `canCancel`. Confirmed over REST that a compose deployment cancels and an unknown id still 404s. **Build from source landed** (migration 0030: `compose.build_enabled`, `compose.build_args` encrypted): the rule is *Nixploy builds, compose runs*. `modules/compose/build.ts` is pure — it validates a bounded `build:` shape (`context`, `dockerfile`, `target`, `args`) and refuses every escape hatch with a named reason (`dockerfile_inline`, `ssh`, `secrets`, `network`, `cache_from`, `extra_hosts`, `tags`, remote git contexts, and any path with `..`, a leading `/` or `~`), collects the build targets and swaps each `build:` for the `image:` Nixploy produced. `build-runner.ts` drives the ordinary Dockerfile builder per service — so it inherits BuildKit `--secret` handling for credential-shaped build args, the cache-export probe and the hardening baseline — tagging `<appName>-<service>:<deploymentId>` so an older render still resolves its images. `prepareComposeFiles` builds before it renders and only when a real deploy passes a build context, so start/stop/domain resync never rebuild behind the operator. 17 unit tests cover the validation; end to end on the local Swarm a two-service stack built its image, ran `built-with:from-nixploy` proving the stack's build args reached the build, deployed with the rendered file carrying `image:` and no `build:` at all, kept the full hardening baseline on the built service, and refused both a deploy with the switch off and a `context: ../../../../etc` escape — the latter before anything ran. **Left for the next pass:** compose mounts/published ports through the existing `mount.compose_id` / `port.compose_id` FKs (lower priority than it looks — a compose file can already declare named `volumes:` inline, only the panel's Mounts tab is missing), a `compose update` CLI verb (none exists today), and a registry push target for stacks whose server is not the build host.

- **2026-09-17 (roadmap, and v0.3 opens with deploy-any-ref)** — a research pass (the competing panels' feature sets, 12 months of their releases, roadmaps and open PRs; 18 pain points with issue links; 33 top-requested features; 19 adjacent platforms; 22 trends) plus a 149-row inventory of this repo produced [`roadmap.md`](./roadmap.md): four releases (v0.3 finish the loop, v0.4 free the paywall, v0.5 the importer/takeover, v0.6 logs and previews) under one thesis — *everything the others charge for, free, and a one-command door in*. The sequencing turns on one verified fact: **`compose/safety.ts` refuses `build:` (line 444) and host `ports:` (line 572), both of which the competing panels accept**, so an importer would mark most real stacks "needs attention" until compose parity lands. Compose parity is therefore a prerequisite for the importer, not a nice-to-have. Other gaps confirmed in code rather than assumed: the compose "Auto deploy" switch is inert (the webhook handler only queries `applications`), the drop/zip source has readers and no uploader, `certificate.autoRenew` has no consumer, DNS-01 credentials never reach the Traefik service (only the provider name lands in `traefik.yml`), no router calls `.output()`, and `ingestServiceLog`'s only caller is the deploy worker on failure. Measured the live panel for the footprint claim: 427 MiB RSS against the 500 MB–1.25 GB reported for the incumbent — comparable, not yet a headline. **First feature landed: deploy any ref.** `deployment.requested_ref` (migration 0029) carries a branch/tag/sha from `application.deploy({ref})` through the queue claim to `buildRefDeployTarget`, which overrides `branch`/`gitBranch` on the deploy target exactly the way `buildPreviewDeployTarget` already did — so the checkout changes and the stored branch does not, and the next webhook push still builds the configured branch. New `modules/deployment/ref.ts` validates through `assertSafeGitRef` and *refuses* a ref on a docker or drop source rather than ignoring it (silently dropping it would deploy the wrong thing and report success). `application.redeployFromDeployment` rebuilds the exact commit of a past row, which works after the branch moved on or was deleted. UI: a "Deploy a ref" action next to Deploy (hidden for non-git sources) and "Redeploy this commit" on history rows carrying a sha. CLI: `app deploy --ref`, `app redeploy-commit`. Verified in the browser end to end — a tag deploy failed with `fatal: couldn't find remote ref refs/tags/v0.2.9` and a commit rebuild with `upload-pack: not our ref 4f2a9c1…`, which is the proof: the ref reaches git, and the row's `requested_ref` records what was asked while `application.branch` stays `main`. Gates: typecheck 4/4, Biome 0/0, knip clean, 2241 passed / 1 skipped with `DATABASE_URL_TEST`.

- **2026-09-15 (v0.2.9 released and rolled to production)** — the panel pass went out as seven commits, then `tools/release.sh paas --bump patch --yes`. Release workflow: 7 minutes end to end on the native runners (checks 1.5 min, arm64 3 min and amd64 3.9 min in parallel, merge/sign/publish 0.9 min), `:latest` and `:v0.2.9` on the same digest, both platforms present. The VPS went v0.2.8 to v0.2.9 with the release's own checksum-verified `update.sh`; backups first (`pre-update-v0.2.9-*.sql.gz` plus `.env`, `traefik/` and the service spec under `/root/nixploy-pre-v0.2.9-*`). After: image v0.2.9, `/api/ready` 200 in-container and through Traefik, migrations clean, all three tenant workloads untouched. Worth recording because it closes an older loop: **the v0.2.8 maintenance fix is confirmed in production** — zero `prune deployment rows` failures from the live task across three hourly runs, where v0.2.7 failed every hour. The failures still visible in `docker service logs` belong to the old task and are timestamped a minute before that roll; filter by task id before believing them.
- **2026-09-15 (panel UX: the sticky-nav bug, and one table primitive)** — the nav "disappearing" when the theme menu or command palette opened was not a z-index or animation problem: `react-remove-scroll-bar` (under every Radix overlay) locks the page with `overflow: hidden !important` on `<body>`, which makes body a scroll container, and `position: sticky` resolves against the *nearest* one — so the header stuck to the top of the body box, 700px above the fold. Measured it rather than guessed (`headerTop: -729` with the menu open). The lock now goes on `<html>`, which is the element that actually scrolls here, so the page still cannot move, the offset is preserved and body stays a plain block box. The override has to be `html body[data-scroll-locked]`, not `body[...]`: the injected rule is added to a `<style>` at runtime and therefore always wins an `!important` tie on source order, but not on specificity. `scrollbar-gutter: stable` on `<html>` kills the sideways jump the same lock used to cause. **New shared primitive**: `hooks/use-table-view.ts` + `components/ui/table-toolbar.tsx` give any in-memory list search, paging and a distinct no-match row, and both pieces of chrome hide themselves below a threshold so short tables look exactly as before. The derivation is a pure function (`resolveTableView`) because `apps/web` has no React testing library — seven cases cover paging, matching, the filtered/unfiltered distinction and the clamp that used to be the bug in hand-rolled versions. Applied to the lists that actually grow: Docker containers/images/volumes/networks, org members, schedules, backup runs (whose fetch went from 20 to 100 rows, since the older ones were simply unreachable). Not applied to servers, registries, certificates, SSH keys, API keys or destinations — those hold a handful of rows, where the threshold would keep the chrome hidden anyway; one line each adopts it if that changes. Also: project service search now matches description and kind, and the dashboard's failure counts link to the incidents tab. **`SettingsSection` is now two columns from `lg`** — heading and explanation beside the controls rather than above them — which is what actually fixes the 1120px card with a 300px input hugging its left edge. It is one component, so all 65 usages moved at once; sections whose body is a table opt out with a new `wide` prop (23 of them) because a table wants the room and a 15rem label column beside it has nothing to sit next to. `bare` implies `wide`. Below `lg` the markup is unchanged, verified at 375px. Also added counts to the tabs that already run the query that answers them — Docker (containers/images/networks/volumes), Git providers, a project's Services, a service's Domains, and unresolved incidents on Monitoring, badged in red — and put the Git provider tabs in the URL, which was the one tabbed surface still using `defaultValue`. The page-by-page pass then turned up a bug in my own primitive: `TableNoMatch` keyed off `visible.length === 0`, which is also true for a genuinely empty table, so Docker's containers and images rendered "nothing matches ''" stacked on top of their real empty state. It now bails on a blank query (covered by a test) rather than each call site guarding. Consistency sweep off the back of the same pass: sixteen surfaces had hand-rolled an empty state or a load failure with their own padding, radius, and no icon or title — they now share `EmptyState` and a new `LoadError` extracted from `QueryState`, and the audit log's own prev/next pager became the shared `Pagination` the client-side tables render, so there is one pager instead of two that disagreed about wording. A service's deployment history was the only bare block on its tab; it is a card now. **Log viewer redesigned**: it carried two toolbars and seven controls, with follow-latest drawn as `ArrowDownToLine` sitting one button from the real `Download` — two near-identical glyphs meaning different things. Now one row (status, filter, a Levels menu with per-level counts, the errors-only pill, the count, and wrap/follow/overflow), `ChevronsDown` for follow, and copy / download / clear behind the overflow. Lines are numbered against the unfiltered stream so a number still means something under a filter, and the level shows as a 2px rule rather than an `ERR`/`WRN`/`OK`/`INF`/`DBG` chip in front of every line — the chip repeated what the text colour already said and turned the block into a column of tags. **And a real bug it surfaced**: `docker exec` against an image with no shell fails on stderr and closes without an error frame, so the terminal overlay fell through to "Deploy the service first" for a service that was running. `terminal-impl.tsx` now keeps the last 2 KB of output and names the actual cause.
- **2026-09-15 (templates: 86 to 145, and the pins caught up)** — 59 new entries across every category, each one gated the same way the existing catalog is: `catalog.test.ts` (unique kebab ids, YAML parses, suggested domain resolves to a real service, every compose `${VAR}` declared in the env schema and vice versa, compose safety, no host ports) plus a live registry probe of every image. The probe caught exactly one bad pin — `quay.io/hedgedoc/hedgedoc:1` is not a published tag — now `1.12.0-alpine`. Logos were checked the same way: 27 of the new ones had no simple-icons mark (and two borrowed Databricks' logo for unrelated SQL tools), so they point at the self-hosted icon set instead; all 96 slugs and all 43 absolute URLs resolve, and the rendered page reports 145 cards, 144 images, zero broken, one deliberate letter fallback (SQLPad has no mark anywhere). Contrast was measured too, not eyeballed: relative luminance of each brand colour against the tile found eight marks that vanish on one theme — six simple-icons slugs missing from `DARK_GLYPH_SLUGS` (appsmith, dbeaver, directus, langflow, owncloud, trilium), two self-hosted icons that declare no fill at all and so paint pure black (heimdall, infisical — these swap to selfhst's `-light` variant), and one in the other direction, Radarr's #FFCB3D on the light tile, which needed the mirror-image rule `template-logo.tsx` did not have. `duplicati` and `wireguard` measured just above the line and keep their own colour. Existing images were audited rather than assumed: everything resolved, but the shared bases were a major behind (PostgreSQL 16 to 17, Redis 7 to 8, MongoDB 7 to 8, MySQL 8.0 to 8.4, MariaDB 11 to 11.8, PostGIS 17-3.5) and four app pins were badly stale — Audiobookshelf sat on 2.9.0 against 2.36.0 upstream, with Linkwarden, Mealie and Homarr close behind. Supabase and ClickHouse were left alone on purpose: those pins exist because the stack around them has to agree. One knock-on: at ~110 distinct images the anonymous Docker Hub budget became the binding constraint and the probe started reporting `postgres:17-alpine` as missing, so `images.ts` now runs three at a time with six tries and capped exponential backoff, and `images.test.ts` reads the retry budget from the module instead of hard-coding 4.
- **2026-09-15 (production rolled to v0.2.8)** — the live instance went v0.2.7 to v0.2.8 with the release's own pinned `update.sh`, downloaded from the v0.2.8 assets and checked against the release `SHA256SUMS` before being run as root (that is the recipe `docs/install.md` documents, and it is the one to use — not the repo working copy). Backups first: the updater's own `pre-update-v0.2.8-*.sql.gz`, plus a copy of `.env`, `traefik/` and the `nixploy` service spec under `/root/nixploy-pre-v0.2.8-*`. After the roll: image `v0.2.8`, `/api/ready` 200 both in-container and through Traefik (`--resolve server.leet.club:443:127.0.0.1`), migrations applied clean, and all three tenant workloads (code-server, it-tools, a managed postgres) untouched. The updater's two "not reachable through Traefik from this host" warnings are the usual hairpin-NAT false alarm — the `--resolve` probe answers 200. Worth noting for the maintenance fix: the *last* line the old task logged, one minute before the roll, was the `prune deployment rows` failure this release fixes.
- **2026-09-15 (five Dependabot PRs triaged, four landed)** — all five were cut from `d40e07f` (v0.2.6) and sat 16 commits behind, so their green checks were evidence about a tree nobody runs any more; rebased each locally onto current main and re-ran the full gate set before deciding. Landed as two commits rather than four merges, because all five rewrite `pnpm-lock.yaml` and merging one would have conflicted the rest. **#3 + #6 + #7**: react/react-dom 19.3 (the panel was on 19.2.4 and landing on 19.1 — aligned again), motion 13 (its only breaking change, the removal of optional `@emotion/is-prop-valid`, applies to nothing here), nodemailer 10 taken at **10.0.10** rather than the 10.0.3 Dependabot pinned (the patches in between are linear-time address parsing / SMTP reply reassembly and control characters stripped from MIME boundaries), and `@types/nodemailer` dropped because v10 is a TypeScript rewrite that ships its own. The SMTP path was checked against a throwaway in-process SMTP server, not just a typecheck — CI never sends mail. **#5**: TypeScript 7 (the Go native port, npm `latest`) across all four workspaces, which also unified four ranges that had drifted from ^5.7.2 to ^5.9.2; nothing here imports the `typescript` API programmatically and no tsconfig uses an option the port dropped. **#4 closed**: `@types/node` 22 → 26 would describe a runtime we do not ship (`node:22-alpine`, and Node 26 is Current, not LTS) — `dependabot.yml` now ignores its majors the way it already ignores the Docker `node` major. Moving the base image to node:24-alpine is the real decision, still open. Separately: commit `9d316bb` produced **zero** workflow runs because its body spelled out GitHub's CI skip marker while explaining the `[release]` guard — GitHub substring-matches it across the whole message. Amended — and the very next commit tripped the same trap one level down: mentioning `[release]` in prose is a `contains()` match in the `if:` of `ci.yml` and `docker.yml`, so the dependency-bump push skipped its Docker build and `:main` had to be rebuilt by dispatch. Both rules now live in CLAUDE.md as well as `docs/releases.md`. First run of the native-runner `docker.yml`, meanwhile: amd64 3.5 min, arm64 2.8 min, manifest 0.4 min — 4 min wall clock against 17 min for the last QEMU run.
- **2026-09-15 (continuous images off QEMU too)** — `docker.yml` still built both architectures on one amd64 runner with `setup-qemu-action`, so every main push paid the emulated arm64 Next.js build (the same 15-80 min that made v0.2.8 take 82 minutes before `release.yml` was split). It now mirrors the release pipeline: a `build` matrix on `ubuntu-latest` + `ubuntu-24.04-arm` pushing untagged manifests by digest with per-arch GHA cache scopes (`main-linux/amd64`, `main-linux/arm64`), then a `merge` job that assembles `:main` / `:<sha>` with `imagetools create` and cosign-signs the list digest. Two behaviour notes: the tags are applied at merge time, so a half-built push never occupies `:main`, and the `[release]`/`[ci skip]` guard moved onto `build` (a skipped `needs:` skips `merge` too). arm64 is not optional — Ampere/Graviton/Pi installs pull these images.
- **2026-09-14 (the source-build smoke earns its keep, first run)** — the new CI step failed immediately, on a real bug: `ERROR: failed to build: Cache export is not supported for the docker driver`. The per-app layer cache is on by default and buildx's default `docker` driver cannot export one, so **every Dockerfile build on a stock Linux Docker failed** — it did not degrade to an uncached build, it died. It passed locally because Docker Desktop ships the containerd image store, which makes the docker driver export cache fine; the user's VPS happens to have it too (checked), so their builds were unaffected, but a plain install is not. `supportsBuildCacheExport()` in `builders/buildx.ts` probes the driver (`buildx inspect` → "Driver:") and falls back to `docker info -f '{{.DriverStatus}}'` for the containerd snapshotter, cached per host like `hasBuildx`; `prepareBuildCache` skips the flags with a hint instead of failing. Five tests cover it. This is the second builder bug in two days that only prebuilt-image smokes could miss — the step stays.
- **2026-09-14 (maintenance bug + source-build smoke — waves 12-15)** — the hourly `[deployment-maintenance] Maintenance step "prune deployment rows" failed` line (seen on the live instance, 6 times in 6 hours) turned out to be real and universal: `pruneDeploymentRows` interpolated a JS `Date` into a `db.execute` raw statement, and postgres-js's unsafe path refuses one (`ERR_INVALID_ARG_TYPE`), so **no install has ever pruned deployment rows or their log files**. Fixed by passing `cutoff.toISOString()::timestamptz`. Two supporting changes: the maintenance logger now appends the driver's `cause` (drizzle's message is just the SQL + params, which is why the log said nothing useful for weeks), and a new `maintenance.db.test.ts` runs the statements against real Postgres — the existing unit test mocks `db.execute`, which is exactly how a query that never worked stayed green. Verified the new test fails on the old code with ERR_INVALID_ARG_TYPE. Swept every other raw `db.execute` for non-string params: this was the only one. **CI gap from the v0.2.7 postmortem closed**: `tools/golden-path-api.mjs` grew an opt-in source-build step (`SMOKE_BUILD_REPO` / `SMOKE_BUILD_TYPE` / `SMOKE_BUILD_BRANCH` / `SMOKE_BUILD_DOCKERFILE`) and `ci.yml` sets it against `docker/welcome-to-docker`; ran locally, 16/16 green, the build itself 46 s. Also: rollbacks "Version" → "Image tag", dark-mode pass over every surface these waves touched.
- **2026-09-14 (panel UX, fifth pass — waves 9-11)** — audit trail: deploy rows carried no `targetName`, so the most common action in the log read as a bare uuid (fixed in `application.deploy` / `compose.deploy`; 17 other audit writes still omit it, most legitimately), and the table had its own 12-hour `toLocaleString` instead of `<DateTime>`. Incidents link to the service that failed — the deploy-failure writer now records `serviceKind` in metadata, which is what turns the ids it already stored into a URL (older rows fall back to plain text). Stress-tested layout with a 55-character service name and a long description: header, breadcrumb and table hold up; capped the services-table name column anyway. Checked polling (`refetchInterval`) across the panel: every one is either a time series, a conditional while-in-flight function, or the 5-minute update banner — no violations of the one-shared-query rule. Auth pages and the template deploy sheet reviewed, nothing to change.
- **2026-09-14 (panel UX, fourth pass — waves 3-8)** — service context: inherited org/project/environment variables are listed read-only under the service's own editor (`components/services/inherited-env.tsx` on `project.getResolvedEnvironment`, override badges, works for databases by resolving the environment name from `environment.byProject`), and the header carries "Deployed <when> by <who>". Settings: page descriptions wrap instead of truncating (the Servers page cut its own instructions mid-word), cards stopped repeating page titles, Platform → Access saves the Let's Encrypt email from a button next to the field. Logs: errors-only filter + copy, and the classifier now treats a bare "failed" as an error — it was hiding "Deployment failed: …", the one line the filter exists to surface. Docker: sub-tabs are `useSyncedTab`-backed so `?tab=volumes` works, and long container/network/volume names truncate (they ran across neighbouring columns and pushed the row off the table — the containers list now fits at 800 px). A11y sweep with three scripted detectors (icon-only buttons, `target="_blank"` without `rel`, `<Label>` with no association): the first two came back clean, the third found 17 real cases, all fixed; the detector now returns zero. Gates: typecheck 4/4, Biome 0/0, web 69.
- **2026-09-14 (panel UX, third pass)** — the audit's two remaining "med" items plus what driving the panel turned up. F6: the create-application dialog takes a Docker image or a repository URL up front, saves it with `application.saveSource` and opens the new service (create used to leave an app that needed two more forms on two more tabs; creating a compose service navigates to it too). F7: a new `domain.checkDns` procedure (resolve4/resolve6 + `detectPublicIp`, rate-limited per org) drives a live hint in the add-domain dialog — resolves / resolves elsewhere / does not resolve, with this server's IP — the certificate picker only renders when something terminates TLS and is cleared when HTTPS is switched off, and the traefik.me generator is a labelled "Test domain" button instead of an icon. Env saves now say they apply on the next deployment and put a Deploy button in the toast (`EnvEditor` gained `footerHint`). New `ServiceLoadError` replaces three hand-rolled "not found" branches: back-link always, Retry only for 5xx/network (a deleted service offered a Retry that could never succeed and no way out). Monitoring leads with the fleet (Prometheus card moved below). Backups empty state carries the button. **Found while looking**: the org switcher overlapped the nav links between ~850 and 1000 px (the shadcn Button base is `shrink-0`), and the instance-backup platform alert named "Settings → Backups", a page that does not exist under that name. Gates: typecheck 4/4, Biome 0/0, knip clean, `pnpm test:db` 1927 passed / 1 skipped, web 69.
- **2026-09-14 (panel UX, second pass)** — drove the panel locally against seeded data (two projects, five services, a domain, real deployments) at 1440 px and 375 px, light and dark. Landed: the service address is on the service header and in the services table (`lib/service-url.ts`, `components/services/service-url.tsx`, `domain.all({projectId})` feeds the table in one query); dashboard project rows show failing/running counts (`countByEnvironment` now groups by status, so `EnvironmentServiceCounts.byStatus` comes free — no extra query); a getting-started checklist backed by a new `project.onboarding` procedure (UX audit F5, the last unlanded top-10 item); the deploy readiness reason is printed under the service title instead of only living in the disabled button's tooltip; **Redeploy removed from the application and compose headers** — it queued the same job as Deploy (`type` only changes the row title), so two buttons implied a difference that did not exist; deployment status labels/colours unified on `lib/status.ts` ("Failed", blue running / green succeeded); the log drawer re-reads its row from the list so it stops saying "Running" under a finished log; `scroll-shadow-x` in `globals.css` on every table and tab strip; deployments table folds Created/Duration under the title below `md`; settings nav becomes a select below `md` (UX audit F19); project toolbar wraps; deployments chart gets an empty state; sentence case on three stragglers. **Two real bugs found while looking**: every dashboard load threw a React hydration error (the recent-deployments subtitle branched on `isPending`, which differs between the prefetched server render and the client's first paint), and three surfaces rendered `formatDistanceToNow` directly instead of `<DateTime>` (which is the component that suppresses the clock-drift mismatch). Gates: `pnpm typecheck` 4/4, Biome 0/0, `pnpm test:db` 1927 passed / 1 skipped, web 69.
- **2026-09-10 (audit)** — Initial audit. Added `CLAUDE.md`, `docs/codebase-map.md`, this file.
- **2026-09-10 (sprint 1, branch `chore/sprint-1-hygiene`)** — hygiene, routine bumps, better-auth 1.7.3, Next 16.3.4 + `src/proxy.ts`, landing → Next 16, commander 15, vitest 5. All gates green.
- **2026-09-14** — after the user cleaned the checkout (node_modules, .next removed; `pnpm install` at the root restores it): external DB connection URLs use the host's public address (`modules/cluster/public-host.ts`, `NIXPLOY_PUBLIC_HOST`); database partial updates no longer inherit the create-time default image (zod `.partial()` keeps `.default()` — a rename on Postgres 18 was refused as a downgrade to 17). Released as v0.2.2. Same day, v0.2.3: the updater compared the installer-pinned tag with itself and always said "up to date" — it now follows the newest GitHub release under the pin.
- **2026-09-14 (source builds broken in the image)** — the runtime image installed `docker-cli` without `docker-cli-buildx`, so every nixpacks/railpack/Dockerfile build failed ("the buildx component is missing or broken") while prebuilt-image deploys worked — which is exactly what CI's golden path covers, so nothing caught it. Fixed in v0.2.7 (plugin in the image + `hasBuildx()` fallback). **CI gap closed 2026-09-14**: `tools/golden-path-api.mjs` takes `SMOKE_BUILD_REPO` / `SMOKE_BUILD_TYPE` and builds one app from source (clone → Dockerfile → Swarm), and `ci.yml` sets them — verified locally at 46 s against `docker/welcome-to-docker`.
- **2026-09-14 (updater + GitHub App, from the live instance)** — v0.2.3 made version-pinned installs follow GitHub releases; v0.2.5 fixed the GitHub App flow (creating an App no longer fails on the missing installation, panel shows Installed/Not installed with an Install button and auto-syncs on the `setup_action=install` return); v0.2.6 fixed the tracked image going stale after a shell `update.sh` roll — the dialog offered `:v0.2.1` on a v0.2.3 host and would have downgraded it. Checks now adopt the running tag, persist the resolved `targetImage`, and `applyUpdate` blocks an implicit downgrade.
- **2026-09-14 (live-instance fixes, SSH access)** — the user gave Claude a dedicated SSH key for the production VPS (`ssh nixploy`, host `173.249.39.210`, root; `~/.ssh/nixploy-vps` + a `Host nixploy` block in `~/.ssh/config` on the Mac), so failures can be diagnosed on the box directly. Found that way: Traefik's buffering middleware on the panel's `/api/` router answered `500 Internal Server Error` for every EMPTY-body response (`vulcand/oxy/buffer: failed to read response, err: no data ready`) — the GitHub App callback's 307, empty 404s, 204s; JSON responses passed, which is why it looked random. Middleware removed from the writer (`modules/traefik/dashboard.ts`) and from the live config. Also: the updater's readiness probe now asks the new task in-container first (a working update was reported as failed because `BETTER_AUTH_URL` was the bare IP while a dashboard domain was configured, so the proxy probe 404'd).
- **2026-09-12 (release day)** — `main` fast-forwarded onto the stability branch (both work branches deleted), v0.2.0 changelog + upgrade notes (`411451d`), versions bumped to 0.2.0 (`a662172`, `e6c0b7c`). `@nixploy/cli@0.2.0` published to npm (workflow_dispatch). The PaaS release pipeline was exercised for real and fixed along the way: `[skip ci]` on the tagged commit skipped the Release workflow (GitHub applies it to tag pushes — now a `[release]` marker, `8181f48`, and a message merely *mentioning* the marker is skipped too, `bc2635c`); `tsx` was missing from `packages/server` so the CI drift step exited 254 silently (`85f925e`); Trivy blocked on node-tar inside the base image's npm (npm removed from the runtime image) and on the esbuild Go TLS finding (`.trivyignore`, `ec29e6b`); the runtime image lacked `openssl` so a self-provisioned Traefik never got its default certificate (`0b51707`); MinIO moved to quay.io (`b7f123b`); the UI golden path resolved Playwright through NODE_PATH, which ESM ignores (`ec29e6b`); a backup right after a database starts failed with "exited with status 1" — dumps now surface stderr and retry while the database is starting (`9dca9b7`). Dependabot: pnpm pinned via `packageManager`, Node major image bumps ignored, PR #1 closed, PR #2 (actions SHA bumps) rebased. Also fixed on the local run: stale-cookie redirect loop (`06f2eaf`), duplicate multi-arch image rows (`9c0d43e`). Local golden path (production build on :3100): 15/15 including Traefik routing once Docker Desktop had been restarted.
- **2026-09-11 (final wave, tooling)** — `aaa7cef` shared tsconfig base (`noUncheckedIndexedAccess` fixed 20 web spots + one server bug), Biome `--error-on-warnings` + 21 stricter rules (0/0), knip 5.88.1 (8 dead files, 16 dead exports; `closeListener` wired into shutdown — the LISTEN connection was never closed), 18 router re-wraps deleted, `CommandError` on `DomainError`, shared `modules/services/app-name.ts`, `invalidateDockerListings` on every lifecycle mutation, server terminal on the SSH pool, `safeNextPath` keeps queries; `7b8d9f0` three unused dependencies dropped. Still open by decision: the `redacted` flag on `one` responses (API shape change on ~30 procedures); F14 over-exported internals / barrel re-exports (one knip flag each); `modules/ai` and `backups/runner.ts` still throw plain `Error` (four `ai.*` and three backup re-wraps remain for that reason).
- **2026-09-11 (final wave, tail)** — `3383608`/`7b38d6c` landing docs sync + doc corrections; `a447c69` compose pull-request previews (migration 0028, one `PreviewParent` lifecycle, shared `preview-deployments-panel.tsx`; found and fixed: every compose domain routed to `http://<app>-<svc>-<svc>-1`, NXDOMAIN); `4f7118f` local git clones were refused by simple-git since the hardened git env landed (`GIT_CONFIG_COUNT` / `protocol.allow` guards — remote clones were unaffected; `hardenedSimpleGit()` opts in, `gitProcessEnv()` forwards only PATH/HOME/USER/LANG/TMPDIR/SSH_AUTH_SOCK) and the provider webhook dispatches compose previews. Tooling strictness (tsconfig base, Biome 0/0, knip, router re-wraps, `CommandError` on DomainError, executor dedupe) in flight.
- **2026-09-11 (final wave of the audit, part 2)** — `33f68e6` TCP/UDP routing + entrypoints (migration 0026; `toTraefikDomainEntry` at every Traefik sync site, `isUniqueViolation` hoisted — `error.code === "23505"` never matched through drizzle and leaked raw SQL), curated engine versions + logical databases; `8bd48ab` compose containers / database status polls gated on the events socket; `3383608` landing docs synced (21 pages, generated 390-endpoint API catalog, derived help-link slugs + test); `7b38d6c` doc corrections from that sync (per-router HTTPS redirect, `NIXPLOY_DEPLOY_CONCURRENCY` default 1, 44 routers, OpenAPI tags). Gates on the final tree: typecheck 0, Biome 1 pre-existing warning, `pnpm test:db` 1853 passed / 1 skipped, web/landing/cli builds, CLI 91 tests, web 63 tests, Docker image build, browser pass over the new surfaces light + dark with 0 console/page errors. **REST e2e 59/65**: the six misses are the Traefik route probes (502) — from inside the Traefik container the whoami *task* IP answers but every service *VIP* on `nixploy-network` is unreachable, from an untouched older container too, after the entrypoint test recreated the Traefik task twice with a failed gateway bind. Environmental (Docker Desktop overlay LB state), not code; the same script was 65/65 earlier the same day. Recreating `nixploy-network` (detach the four services, `docker network rm/create --driver overlay --attachable`, re-attach) or restarting Docker Desktop, then `e2e.py all`, is the re-check.
- **2026-09-11 (final wave of the audit, part 1)** — `d61a238` pooled SSH transport + per-server breaker + bounded fan-out (`utils/ssh-pool.ts`, `utils/fan-out.ts`, `server.transportState`; the reconciler's plain-compose probes had inherited the 30 min command budget, one dead host wedged the every-minute pass), `1629068` process roles (`NIXPLOY_ROLE` all/panel/worker, `apps/web/worker.ts`, `install.sh --split-worker`, Postgres LISTEN/NOTIFY, `/ws/events` push + `use-live-events`; found: the job runner was only registered as a side effect of importing `modules/deployment/index.ts`), `2133d01` follow-ups (429 + Retry-After for throttled keys — better-auth's own throttle answered like a revoked key —, `organization.list`, audit `since`/`until`/export, auth events on the ip/user-agent columns, egress toggle UI, dashboard empty-state race: query-core de-duplicated the invalidation onto the in-flight empty-state request), `aeee855` compose rollbacks (`compose_deployment_snapshot`, service-level env restored) + template sources + updater release notes/pin + image schedules (migration 0027), `89e4731` nested routers reachable over REST (`walkProcedurePath`, was `caller[first][rest]`), `fac1d38` UI golden path against the four-step wizard, `256c461` docs, `9415096` preview commit metadata + self-hosted commit links (`modules/git/commit-url.ts`, `compose.one` loads provider base URLs only) + `NIXPLOY_METRICS_RETENTION_HOURS` + `GET /api/metrics` + `/ws/server-terminal` + volume file browser (`volumeFiles.*`, realpath-confined). Routing/DB landed in part 2 below.
- **2026-09-11 (Sprint E of the audit)** — `d3f73d0` splits (`git/webhook-handler`, `databases/engine`, `routers/application` + shared `test-utils`), `1b9cd0d` CI (real e2e job, UI golden path, cosign, SHA-pinned actions, installer checksums, unit/db vitest projects with `fileParallelism: false`), `2e25d90` install checksum docs, `267aefd` web forms (shared `useDraft`/`useSaveMutation`/`useSaveBar`, mounted `SaveBarProvider`, merged schedules/backups panels, `apps/web` vitest 40 tests), `7cb67ee` hardening (migration 0025: pinned egress + `allowPrivateEgress` toggle, `ENCRYPTION_KEYS` rotation + `nixploy:rotate-key`, build secrets off argv, streaming DB backups, audit ip/user-agent/export, peer-IP rate-limit check, 0600 file modes), `15c2126` manual tab activation + `nxp_` docs. Gates: typecheck 0, Biome 1 pre-existing warning, `pnpm test:db` 1559 passed / 1 skipped, web/landing/cli builds, CLI 83 tests, web 40 tests, Docker image build, REST e2e 65/65. Final wave launched (worker split + `/ws/events`, SSH pool, TCP/UDP routing + DB version picker + logical DBs, compose rollbacks + template sources + updater notes/pin + image schedules, preview commit metadata + Prometheus export + server terminal + volume browser, CLI/web follow-ups).
- **2026-09-11 (Sprints C + D of the audit)** — migrations 0021 (middlewares, compose redirects/security, ACME DNS), 0022 (`privileges_swarm`), 0023 (queue `app_name`/preview id, `last_run_at`), 0024 (hooks, preview knobs, push registry, auto-update, incident ack, status page). Network model `nixploy-internal` / per-environment overlays / domain-gated `nixploy-network`; container baseline (caps dropped, no-new-privileges, pids, quota limits); DB-backed queue with SKIP LOCKED; append-only metrics; one service page frame; platform self-alerts; HTTPS opt-out; DR restore test; uninstall.sh; CLI/MCP/OpenAPI parity; scoped keys + SSO; convergence gate. Gates: typecheck 0, Biome 1 pre-existing warning, 1382 tests with Postgres (run alone — the DB-backed queue test flakes when two suites share `nixploy_test`), web/landing/cli builds, CLI 83 tests, Docker image build, REST e2e 65/65.
- **2026-09-11 (Sprint B of the audit)** — migration 0020 (deployment provenance + `backup_run`); error boundary (`DomainError`, one formatter, Biome rule, `describeError` toasts); deploy provenance + pre-flight + shared running-deployments hook + build-args-only builds; backup run history, local destination, restore verification; half-built features surfaced (shared org env, .env import/export, duplicate/move, watch paths, Swarm forms, git-provider edit, tags, GitOps sync, server history chart, notification emits), six dead procedures removed. Gates: typecheck 0, Biome 1 pre-existing warning, 1086 tests with Postgres, web build, REST e2e 65/65.
- **2026-09-11 (Sprint A of the audit)** — CI gate + PR builds + multi-arch + scans; four security Highs closed + input caps + headers; `queued` status, per-app mutex, graceful shutdown, timeouts, offset log follower (migration 0019); health/ready/version endpoints, pre-upgrade dump, downgrade guard, retention, atomic Traefik writes; panel UX safety batch. Gates: typecheck 0, Biome 1 pre-existing warning, 986 tests with Postgres, web/landing/cli builds, Docker image build, REST e2e loop all green, real SIGTERM drain verified. Leftover Swarm service from the smoke: `demo-app-4ae3a9` (nixploy_e2e's demo-app).
- **2026-09-11 (improvement audit)** — six parallel read-only audits (security posture, architecture/scale, product gaps versus competing panels, ops/DX, code health, panel UX) → `docs/audits/2026-09/` with a sequenced plan in its README. Nothing fixed yet.
- **2026-09-10/11 (stability sweep + landing, branch `work/stability-and-landing`)** — 7 parallel audits → fixes per scope (see "What the stability sweep did"), remote-server redesign (`swarm_node_id` + `node.id` pinning, migration 0018), landing rebuilt then redesigned monochrome (2026-09-11), docs updated. Gates: typecheck 0/0, 886 tests, Biome clean, builds green, REST e2e loop 58/59 (resume artifact), UI driven light + dark. Nothing pushed.
